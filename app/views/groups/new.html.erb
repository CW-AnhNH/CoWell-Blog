<h1>New group</h1>

<% unless current_user.admin? %>
  <%= render "form_user", group: @group %>
<% else %>
  <%= render "form", group: @group %>
<% end %>
<br>

<div>
  <%= link_to "Back to groups", groups_path %>
</div>
